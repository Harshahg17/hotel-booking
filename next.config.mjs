/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  
    images: {
      domains: ['images.pexels.com'],
      domains: ['media.istockphoto.com'], // Add the necessary domains here
    },
  
};

export default nextConfig;
